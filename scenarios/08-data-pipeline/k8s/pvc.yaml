apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-08-data
  namespace: wf-scenario-08
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "08"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
