apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-20-data
  namespace: wf-scenario-20
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "20"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
