apiVersion: v1
kind: Namespace
metadata:
  name: wf-scenario-55
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  namespace: wf-scenario-55
data:
  app.yaml: |
    # loaded from config/app.yaml via make deploy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario-55
  namespace: wf-scenario-55
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario-55
  template:
    metadata:
      labels:
        app: scenario-55
    spec:
      containers:
        - name: workflow
          image: workflow-server-datadog:local
          imagePullPolicy: Never
          args: ["-config", "/config/app.yaml", "-data-dir", "/data"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
        - name: mock-datadog
          image: mock-datadog:local
          imagePullPolicy: Never
          env:
            - name: MOCK_PORT
              value: "19055"
          ports:
            - containerPort: 19055
      volumes:
        - name: config
          configMap:
            name: app
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-55
spec:
  selector:
    app: scenario-55
  ports:
    - port: 8080
      targetPort: 8080
