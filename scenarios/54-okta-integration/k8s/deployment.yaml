apiVersion: v1
kind: Namespace
metadata:
  name: wf-scenario-54
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  namespace: wf-scenario-54
data:
  app.yaml: |
    # loaded from config/app.yaml via make deploy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario-54
  namespace: wf-scenario-54
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario-54
  template:
    metadata:
      labels:
        app: scenario-54
    spec:
      containers:
        - name: workflow
          image: workflow-server-okta:local
          imagePullPolicy: Never
          args: ["-config", "/config/app.yaml", "-data-dir", "/data"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
        - name: mock-okta
          image: mock-okta:local
          imagePullPolicy: Never
          env:
            - name: MOCK_PORT
              value: "19054"
          ports:
            - containerPort: 19054
      volumes:
        - name: config
          configMap:
            name: app
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-54
spec:
  selector:
    app: scenario-54
  ports:
    - port: 8080
      targetPort: 8080
