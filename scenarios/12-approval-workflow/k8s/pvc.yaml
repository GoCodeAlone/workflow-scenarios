apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-12-data
  namespace: wf-scenario-12
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "12"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
