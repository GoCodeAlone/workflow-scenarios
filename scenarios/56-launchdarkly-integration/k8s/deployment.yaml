apiVersion: v1
kind: Namespace
metadata:
  name: wf-scenario-56
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  namespace: wf-scenario-56
data:
  app.yaml: |
    # loaded from config/app.yaml via make deploy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario-56
  namespace: wf-scenario-56
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario-56
  template:
    metadata:
      labels:
        app: scenario-56
    spec:
      containers:
        - name: workflow
          image: workflow-server-launchdarkly:local
          imagePullPolicy: Never
          args: ["-config", "/config/app.yaml", "-data-dir", "/data"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
        - name: mock-launchdarkly
          image: mock-launchdarkly:local
          imagePullPolicy: Never
          env:
            - name: MOCK_PORT
              value: "19056"
          ports:
            - containerPort: 19056
      volumes:
        - name: config
          configMap:
            name: app
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-56
spec:
  selector:
    app: scenario-56
  ports:
    - port: 8080
      targetPort: 8080
