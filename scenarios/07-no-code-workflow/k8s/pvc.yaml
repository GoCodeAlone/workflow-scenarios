apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-07-data
  namespace: wf-scenario-07
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "07"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
