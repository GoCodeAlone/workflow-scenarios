apiVersion: v1
kind: Namespace
metadata:
  name: wf-scenario-60
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  namespace: wf-scenario-60
data:
  app.yaml: |
    # loaded from config/app.yaml via make deploy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario-60
  namespace: wf-scenario-60
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario-60
  template:
    metadata:
      labels:
        app: scenario-60
    spec:
      containers:
        - name: workflow
          image: workflow-server-slack:local
          imagePullPolicy: Never
          args: ["-config", "/config/app.yaml", "-data-dir", "/data"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
        - name: mock-slack
          image: mock-slack:local
          imagePullPolicy: Never
          env:
            - name: MOCK_PORT
              value: "19060"
          ports:
            - containerPort: 19060
      volumes:
        - name: config
          configMap:
            name: app
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-60
spec:
  selector:
    app: scenario-60
  ports:
    - port: 8080
      targetPort: 8080
