apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-enterprise-rag-pipeline
spec:
  type: ClusterIP
  selector:
    app: workflow-server
  ports:
    - port: 8080
      targetPort: 8080
