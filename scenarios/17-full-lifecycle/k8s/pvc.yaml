apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-17-data
  namespace: wf-scenario-17
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "17"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
