apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-15-data
  namespace: wf-scenario-15
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "15"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
