apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-16-data
  namespace: wf-scenario-16
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "16"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
