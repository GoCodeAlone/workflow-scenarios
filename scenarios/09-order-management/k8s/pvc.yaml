apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-09-data
  namespace: wf-scenario-09
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "09"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
