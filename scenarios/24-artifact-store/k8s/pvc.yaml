apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-24-data
  namespace: wf-scenario-24
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "24"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
