#!/usr/bin/env bash
# Scenario 29 — GitLab CI plugin (mock mode)
# No persistent seed data required; the gitlab.client uses mock:// mode.
echo "Scenario 29: no seed data required (mock GitLab client)"
