apiVersion: v1
kind: Namespace
metadata:
  name: wf-scenario-59
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  namespace: wf-scenario-59
data:
  app.yaml: |
    # loaded from config/app.yaml via make deploy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario-59
  namespace: wf-scenario-59
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario-59
  template:
    metadata:
      labels:
        app: scenario-59
    spec:
      containers:
        - name: workflow
          image: workflow-server-discord:local
          imagePullPolicy: Never
          args: ["-config", "/config/app.yaml", "-data-dir", "/data"]
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
        - name: mock-discord
          image: mock-discord:local
          imagePullPolicy: Never
          env:
            - name: MOCK_PORT
              value: "19059"
          ports:
            - containerPort: 19059
      volumes:
        - name: config
          configMap:
            name: app
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-server
  namespace: wf-scenario-59
spec:
  selector:
    app: scenario-59
  ports:
    - port: 8080
      targetPort: 8080
