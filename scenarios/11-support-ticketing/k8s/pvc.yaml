apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-11-data
  namespace: wf-scenario-11
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "11"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
