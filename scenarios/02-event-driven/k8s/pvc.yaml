apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenario-02-data
  namespace: wf-scenario-02
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: workflow-server
    scenario: "02"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
